import React, { Component } from "react";
import { View, Text } from "react-native";
import { MainScreen } from "./MainRoot";

console.disableYellowBox = true;

export default class componentName extends Component {
  render() {
    return <MainScreen />;
  }
}
